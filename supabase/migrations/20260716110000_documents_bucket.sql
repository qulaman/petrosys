-- =============================================================================
-- QuarryOps · Миграция 0010 — приватный бакет documents для сформированных актов
-- Загрузка/выдача — только сервером через service_role (действия под ролью
-- office/admin), поэтому объектные политики не нужны: бакет закрыт по умолчанию.
-- =============================================================================
insert into storage.buckets (id, name, public, file_size_limit)
values ('documents', 'documents', false, 26214400)  -- ≤ 25 МБ
on conflict (id) do nothing;
