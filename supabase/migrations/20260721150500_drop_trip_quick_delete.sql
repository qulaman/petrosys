-- =============================================================================
-- QuarryOps · Миграция 0024 — снятие 5-минутного окна отмены рейса
-- С двухэтапным вводом правило одно: в ОТКРЫТОЙ карточке смены учётчик удаляет
-- свои рейсы свободно, в закрытой — только офис/админ. Старая политика
-- «5 минут после записи» позволяла удалить свежий рейс даже в закрытой карточке.
-- =============================================================================

drop policy if exists "own trip quick delete" on public.trip_records;
